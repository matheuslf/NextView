<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="desenvolve.nextcode.next">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.READ_PHONE_STATE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:usesCleartextTraffic="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name="desenvolve.nextcode.next.LoginActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name="desenvolve.nextcode.next.MainActivity" android:screenOrientation="portrait"/>
        <activity android:name="desenvolve.nextcode.next.CampanhaActivity" android:screenOrientation="portrait"/>
        <activity android:name="desenvolve.nextcode.next.MinhaCampanhasActivity" android:screenOrientation="portrait"/>

    </application>

</manifest>